# AA 项目修复记录 · 2026-09-14

针对 AA 课程的一次全量代码审查与修复。所有结论都经实测确认，附复现命令。

---

## 一、修复的问题

### P0 · Day4 框架跑不了并发，而教程在推荐它

**现象**（复现）：

```bash
cd day04-搭建框架/framework
pytest -n 2      # → 3 failed, 22 passed
pytest -n 4      # → 4 failed, 21 passed
```

报错：`KeyError: 变量 ${order_id} 不在变量池中`

**根因**：`pytest-xdist` 的 `-n` 开的是**进程**不是线程，每个 worker 内存独立。
框架用 `VarPool` 全局变量池传接口依赖（用例 A 提取 `order_id` → 用例 B 用 `${order_id}`），
并发时两条用例落在不同进程，B 取不到值。

**文档层面的矛盾**（比代码问题更严重）：
- `day04/教程.md` 第六节表格推荐 `pytest -n 4`
- 同一篇教程 Q4 又说"用例不依赖执行顺序——因为一旦要并发执行，有顺序依赖就会乱"

**修复**：
1. `conftest.py` 的 `pytest_configure` 里加并发拦截，给出原因说明而不是甩一屏 KeyError
2. `day04/教程.md` 新增小节「为什么 `-n` 并发用不了」，含三条改进路径 + 面试答法
3. `day04/教程.md` Q4、Q7 话术改为与实际情况一致
4. `day03/教程.md`、`day05/追问清单与答法.md` 同步补充前提说明
5. `README.md` 新增「已知限制」

**修复后**：

```
pytest -n 2   →  友好提示 + 退出码 1，无 Traceback
pytest        →  25 passed
```

---

### P1 · `--env=prod` 的失败提示指错方向

**现象**：指定 `prod` 环境（`https://api.example.com`）连不上时，
提示写着"连不上 mock 服务！请先另开终端执行 python mock_server\app.py"。
但用户已经切到远程环境了，去启动 mock 服务毫无意义。

**修复**：`conftest.py` 的 `check_service` 按 base_url 分流提示：
- 本地地址（127.0.0.1 / localhost）→ 提示启动 mock 服务
- 其他地址 → 提示"这不是本地 mock 服务，连不上通常是正常的"

---

### P1 · 环境名写错时的报错不可读

**现象**：`python run.py --env=nosuchenv`

```
25 errors in 0.35s
KeyError: "配置里没有环境 nosuchenv，可选：['test', 'prod']"
```

25 条用例全部 ERROR，用户看到的是一屏堆栈，不知道是自己写错了环境名。

**修复**：`pytest_configure` 里提前校验并 `pytest.exit`：

```
环境名无效："配置里没有环境 nosuchenv，可选：['test', 'prod']"
请检查 config.yaml 里的环境名，或去掉 --env 直接用默认环境。
```

---

### P1 · 两个模块写了自测块但跑不起来

**现象**：

```bash
python common/request_util.py   # ModuleNotFoundError: No module named 'common'
python config/config.py         # 同上
```

**根因**：Python 直接运行脚本时 `sys.path[0]` 是**脚本所在目录**（`common/`），
不是 framework 根目录，所以 `from common.xxx import` 找不到包。

> 说明：`case_runner.py` / `base_api.py` / `user_api.py` / `order_api.py` 直接运行也会报
> ModuleNotFoundError，但那几个文件本来就没写 `if __name__ == "__main__"` 自测块，
> 单独运行没有意义 —— 那是设计如此，不是 bug。

**修复**：这两个文件顶部加路径引导，并且只在直接运行时生效：

```python
if __package__ in (None, ""):
    sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
```

---

### P2 · 框架里有两份 client，其中一份没登录

**现象**：`conftest.py` 的 `client` fixture 用 `RequestClient()` **新建实例**，
而 `api/base_api.py` 用 `get_client()` **单例**。两者是不同对象。

token 只 set 在 fixture 那一份上。所以任何忘记注入 `client` fixture 的用例，
会走单例、拿到**未登录**的 session，返回 401，而报错信息完全指不出原因。

当前 25 条用例都注入了 fixture，所以没暴露 —— 但是个定时炸弹。

**修复**：`conftest.py` 的 fixture 改为 `get_client()`，与业务层共用同一个实例；
新增 `reset_client()` 在 teardown 时关闭并释放单例。

---

### P2 · mock 服务开着 debug 模式

**现象**：`mock_server/app.py` 里 `debug=True`，Flask 会启动 reloader（额外 fork 一个进程），
导致启动日志打印两遍、容易被误解为程序出错。

**修复**：改为 `debug=False` 并注释说明原因。

---

## 二、实测确认**没有**问题的项

审查时一并验证了这些，结论是好的：

| 检查项 | 结果 |
|---|---|
| Day1 五个脚本 | 全部跑通 |
| Day2 五个脚本 | 全部跑通 |
| Day3 用例（串行） | 49 passed, 2 skipped, 4 xfailed, 1 xpassed |
| Day3 用例（`-n 4`） | **49 passed —— 并发正常** |
| Day4 框架（串行） | 25 passed |
| `--env` 参数链路 | 生效（`--env=test` 正常，`--env=prod` 确实切换到了远程地址） |
| 变量池类型保留 | 正确（整串占位符保留 int，部分占位符转字符串） |

**Day3 能并发、Day4 不能**这个对比值得注意 —— 差别不在 pytest 用法，
在于 Day3 的用例是自包含的，Day4 的用例之间有数据依赖。
这正好是讲"并发的前提条件"的现成教材。

---

## 三、修复后全量回归

24 项检查，全部通过：

```
【1】Day1 脚本            5/5
【2】Day2 脚本            5/5
【3】Day3 pytest          2/2（串行 + 并发）
【4】Day4 框架            3/3（串行 25 passed + -n 2/-n 4 友好拦截）
【5】run.py --env         3/3（test / prod / 无效环境名）
【6】模块直接运行          6/6
```

---

## 四、改动文件清单

| 文件 | 改动 |
|---|---|
| `day04-搭建框架/framework/conftest.py` | 并发拦截、环境校验、服务检查提示分流、client 统一为单例 |
| `day04-搭建框架/framework/common/request_util.py` | 加路径引导；新增 `reset_client()` |
| `day04-搭建框架/framework/config/config.py` | 加路径引导 |
| `mock_server/app.py` | `debug=True` → `False` |
| `day04-搭建框架/教程.md` | 并发行加标注；新增「为什么 `-n` 并发用不了」；Q4/Q7 话术更新 |
| `day03-pytest框架/教程.md` | 插件表格加前提；并发问答补具体现象 |
| `day05-工程化与面试/追问清单与答法.md` | 第 15、27、28 题更新 |
| `README.md` | 新增「已知限制」小节 |
