# 5 天 Python 自动化面试冲刺

> 定位：**面试冲刺**，不是系统入门。
> 每一章都会标注 `【面试考点】`——那是面试官真正会追的地方。
> 目标不是"学完所有知识"，而是 **5 天后能扛住 Python 自动化岗的面试，并且有东西可讲**。

---

## 这套课和 dsh 的区别

桌面上已有的 `dsh/api-auto-test` 是 7 天系统课，但它有个缺口：**没有 Python 语言基础**，day1 直接上 requests。
后果是你学完能照着写，但面试官问一句"fixture 为什么用 yield"，你就卡住了。

所以这套课的排法不同：

| | dsh（7天） | AA（5天） |
|---|---|---|
| 起点 | requests | Python 三座大山（装饰器/yield/类） |
| 目标 | 能搭框架 | 能搭框架 **且能讲清楚为什么这么搭** |
| 收尾 | CI 集成 | CI + 面试包装、项目话术、追问预案 |

---

## 5 天安排

| 天 | 主题 | 学完你能干什么 | 面试权重 |
|---|---|---|---|
| **Day 1** | Python 三座大山 | 手写装饰器、讲清 yield、用类做封装 | 高（必考） |
| **Day 2** | requests 接口测试 | 独立写接口请求脚本，处理 token 和签名 | 高 |
| **Day 3** | pytest 测试框架 | 写带前后置、参数化的用例 | 高（必考） |
| **Day 4** | 搭建自己的框架 | 从零分层搭出可运行的框架 | **最高（决定薪资）** |
| **Day 5** | 工程化 + 面试包装 | 接入 Git/Jenkins，把项目讲成故事 | 高 |

每天的结构：**教程（讲 + 考点标注）→ 代码（可直接跑）→ 练习（带答案）**。

---

## 学习方法（重要，别跳）

1. **代码必须手敲**，不要复制粘贴。手敲过和看过是两回事，面试时肌肉记忆只有手敲才有。
2. **先跑通再理解**。每个 `code/` 下的脚本先运行一遍看输出，再回头读教程。
3. **遇到报错别急着问**：先看最后一行错误信息，80% 的问题在那里写着。
4. **Day 1 和 Day 4 是重点**，如果时间不够，Day 3 可以压缩，Day 1 和 Day 4 不能省。
5. 每天结束时，合上文档，**用自己的话复述一遍今天学了什么**。说不出来就是没掌握。

---

## 提前给你打个预防针：5 天能到哪

| 能 | 不能 |
|---|---|
| 手写一个能跑的接口自动化框架 | 精通 Python |
| 讲清框架的每一层为什么这么设计 | 独立解决复杂的技术难题 |
| 应付初中级自动化岗 70% 的追问 | 唬住资深面试官 |
| 把项目经历讲成一个完整的故事 | 伪造项目经验（别干这事） |

**诚实提示**：如果你之前是跟着教程敲完 dsh 的，那这套课里 Day 4 的价值最大——它会逼你回答"为什么"，而这正是你目前最大的风险点。

---

## 目录结构

```
AA/
├── README.md                ← 你在这
├── 00-环境准备.md            ← 先做这个
├── requirements.txt
├── mock_server/
│   └── app.py               ← 课程配套的练习接口（Flask）
├── day01-Python核心/
│   ├── 教程.md
│   ├── 练习与答案.md
│   └── code/
├── day02-requests接口测试/
│   ├── 教程.md
│   └── code/
├── day03-pytest框架/
│   ├── 教程.md
│   └── code/
├── day04-搭建框架/
│   ├── 教程.md
│   └── framework/           ← 最终产物，可直接运行
└── day05-工程化与面试/
    ├── 教程.md
    ├── 项目介绍话术.md
    └── 追问清单与答法.md
```

---

## 快速开始

```bash
# 1. 进入目录
cd <仓库目录>

# 2. 创建并激活虚拟环境（详细步骤见 00-环境准备.md）
# 如果你已经按文档建好了 .venv，直接激活：
.venv\Scripts\activate

# 3. 启动练习用的接口服务（另开一个终端窗口，保持运行）
python mock_server\app.py
# 看到 Running on http://127.0.0.1:5000 就成功了

# 4. 开始 Day 1
```

> mock 服务要先启动，Day 2 之后的代码都依赖它。

---

## 已知限制

写在这里，免得你踩了以为是自己的问题。

| 限制 | 说明 |
|---|---|
| **Day 4 框架跑不了 `-n` 并发** | 框架靠全局变量池在用例间传接口依赖，而 `pytest-xdist` 是多进程、内存不共享。跑 `-n` 会被框架友好拦下并说明原因，**不是你把环境搞坏了**。完整分析在 Day 4 教程「为什么 `-n` 并发用不了」 |
| Day 3 的用例可以并发 | `pytest -n 4` 能跑通（49 passed）—— 因为它没有跨用例的数据依赖。这个对比本身就值得体会 |
| mock 服务数据在内存里 | 重启服务就重置，不持久化 |

---

## 自测：你现在能答出几题？

在开始之前，先试试这几道真题。答不上来不要紧，这就是你这 5 天的目标。

1. 手写一个装饰器，统计函数运行时间。
2. `yield` 和 `return` 有什么区别？pytest 的 fixture 为什么用 yield？
3. `requests.Session()` 有什么用？
4. fixture 的 scope 有哪几种？分别什么时候执行？
5. 你的框架目录是怎么分的？为什么这么分？

（答案分别在 Day 1、Day 2、Day 3、Day 4）
