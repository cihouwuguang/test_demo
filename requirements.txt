# AA 课程依赖
#
# [!] 这些版本是**实测通过的那一套**，请按原样安装：
#     Python 3.13
#     pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 为什么必须和实测环境一致？
#     课程文档里所有"实测数字"（Day3 49 passed / Day4 78 passed 等）
#     都是在这套版本下跑出来的。版本不一致时，行为可能不同——
#     比如 pytest 8 → 9 是跨大版本，插件的钩子签名、收集行为都可能有变化。
#     之前这里写的是一组过期的版本号，装出来是另一套栈，学员会对不上文档。
#
# 怎么核对装对了：
#     pip list        # 逐行比对下面的版本号
#     或者直接跑一遍：cd day04-搭建框架/framework && pytest -q
#     预期结果见 README「快速开始」

requests==2.34.2
pytest==9.1.1
pytest-html==4.2.0
pytest-rerunfailures==16.6.1
pytest-xdist==3.8.0
PyYAML==6.0.3
jsonpath-ng==1.8.0
Flask==3.1.3

# ---------------------------------------------------------------- 可选（课程里没落地）
#
# allure-pytest   报告更丰富（趋势图、分级、步骤）。本课程**没有装**，
#                 Day5 只讲概念和接法，不要把"用了 Allure"写进简历。
# pymysql         连 MySQL 用。Day2 的数据库校验课用 Python 内置 sqlite3 演示，
#                 不需要它；真实项目里装它，写法几乎一样。
#
# 注意：pytest-rerunfailures 装了，但**本框架不能安全地开用例级重试**——
#       重试一条"支付订单"用例意味着重复支付。详见 day04 教程第六节表格下方说明。
