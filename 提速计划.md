# Python 提速：从「看懂」到「写得出来、讲得清楚」

> 篇幅刻意压短。**这份文档是用来照着做的，不是用来读的。**
> 每天真正该花的时间在这儿之外。

---

## 一、为什么慢（诊断）

自学的默认陷阱：**用"看"的进度冒充"会"的进度。**

看教程有流畅感——每页都懂，一小时能翻几十页。写代码全是挫败——到处卡住，一小时写不出二十行。所以人会自动选择前者，还以为自己在学习。

判断标准只有一条：

> **关掉教程，能不能自己写出来？**

能看懂但写不出来 = 没会。面试考的是后者。

**所以提速不是加快看的速度，是把时间从「输入」搬到「输出」。**

---

## 二、三条原则

**① 输入 ≤ 每天 15%。** 一天 3 小时的话，看资料最多 30 分钟。超过就是在自我安慰。

**② 每天的产出是代码，不是页数。** 睡前问自己：今天**写了**什么？答不上来就是没学。

**③ 卡住的地方才是真缺口。** 看教程时"嗯嗯都懂"，关上教程一写就卡——卡住的那个点，就是你面试会挂的地方。**只攻这些点。**

---

## 三、先定位：5 道自测题

**不许查资料、不许翻代码。** 每题给自己 5 分钟。答不上来的就是你接下来要攻的。

| # | 题 | 对应能力 | 结果 |
|---|---|---|---|
| 1 | 手写一个装饰器，统计函数运行时间 | 装饰器 | ☐ 能写 ☐ 卡住 |
| 2 | pytest 的 fixture 里，`yield` 前面和后面的代码分别在什么时候执行？ | yield + fixture | ☐ 能答 ☐ 卡住 |
| 3 | 为什么用 `requests.Session()` 而不是每次 `requests.get()`？ | Session | ☐ 能答 ☐ 卡住 |
| 4 | 下单接口要用登录返回的 user_id，代码上怎么解决？ | 接口依赖 | ☐ 能答 ☐ 卡住 |
| 5 | 打开 day04 框架，说出 common/ 下每个文件负责什么，**以及为什么要有 api/ 这一层** | 框架设计 | ☐ 能答 ☐ 卡住 |

**第 5 题最关键**。前四题是知识点，第五题是「你会不会设计」——面试区分度全在这一题上，也是最难靠看教程补的。

---

## 四、核心方法：反向复现

这是整个计划里最有效的动作，**不要跳过**。

### 操作流程

1. 打开 `day04-搭建框架/framework`，**只看目录结构和文件名**（2 分钟，不看代码）
2. **关掉所有代码文件**，凭自己的理解写 `common/request_util.py`
3. 写不出来、卡住了 → 才允许回去看，**而且只看卡住的那一段**
4. 把卡住的点记在下面的表里

### 为什么有效

看你以为自己都懂 → 一写发现全是洞 → 这些洞才是你真正的缺口。
2 小时的反向复现 > 8 小时的从头再看。

### 卡点记录

| 日期 | 我在写什么 | 卡在哪 | 回去看完之后，原因是什么 |
|---|---|---|---|
| | | | |

**这张表是这轮提速最值钱的东西。** 填满它就等于把"我以为我会"和"实际会不会"的差距全部暴露出来。

---

## 五、两周安排

### 第一周：从「看懂」到「能写」

| 天 | 输出任务 | 过关标准 |
|---|---|---|
| 1 | 默写装饰器 + 自己写 3 个变体（统计耗时 / 带参数 / 重试） | 不查资料 5 分钟写出来 |
| 2 | 自己写一个带 yield 前后置的 fixture，跑通 | 说清 yield 前后何时执行 |
| 3 | 自己写「登录 → 取 token → 下单」链路 | 用 Session，token 复用 |
| 4 | 自己写变量池替换函数（处理 `${}`） | **能保住 int 类型不被转成字符串** |
| 5 | 把第 3、4 题改写成 pytest 用例 | 有参数化、有断言、能跑 |
| 6 | **反向复现** `request_util.py` | 记下卡点 |
| 7 | **反向复现** `config` / `logger` / `assert_util` | 能说出每层为什么存在 |

### 第二周：从「能写」到「能讲」

| 天 | 输出任务 | 过关标准 |
|---|---|---|
| 8 | 给框架加一个你想要的功能（推荐：数据库断言） | 自己想清楚加在哪一层 |
| 9 | 给框架加失败重试 | 说出用插件还是自己写，为什么 |
| 10-13 | 每天挑一个模块，**讲给面试官听**（找我模拟） | 被追问三轮不倒 |
| 14 | 完整模拟面试 60 分钟 | 不崩 |

**第 10-13 天别省。** "能讲清楚每一行为什么这么写"这个能力，只能靠被追问练出来，看书练不出来。

---

## 六、砍掉的部分（提速 = 少做）

**现在不用学的：**

- Day5 的 Jenkins / Allure 实操 —— 面试问到能说出概念和写法就够，不用真搭
  （仓库里**确实没有** CI 配置、也没装 `allure-pytest`，Day5 教程里已统一按"概念 + 写法"来讲，
  并明确写了"别在面试里说自己项目用了 Allure"）
- UI 自动化（selenium / playwright）—— 你投的岗位基本不考
- 性能优化、异步编程、读源码 —— 现阶段全是干扰

**必须攻的就 5 个：**

1. 手写装饰器
2. fixture 的 yield 前后置 + scope
3. 为什么要用 Session
4. 接口依赖怎么解（变量池）
5. **自己框架的分层和为什么这么分**

这 5 个能讲透，面试的 Python 部分就过了。其余的等进了公司再补——那时有真实项目，学得比现在快得多。

---

## 七、每日自检（睡前 30 秒）

- [ ] 今天**写**了代码吗？（不是看）
- [ ] 有没有卡住的地方被记下来？
- [ ] 能不能用一句话说清今天写的东西**为什么这么写**？

第三条答不上，明天就不要往前走，先把今天这个讲明白。
