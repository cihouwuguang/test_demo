# 00 · 环境准备

预计耗时：20 分钟。这一步做扎实，后面 5 天不会卡在环境上。

---

## 一、为什么要虚拟环境

你机器上可能装过别的 Python 项目，它们需要的包版本不一样。
**虚拟环境就是给每个项目一个独立的"包柜子"**，互不干扰。

不用虚拟环境的后果：装了新包把旧项目搞崩，排查起来很痛苦。

---

## 二、步骤

### 第 1 步：确认 Python 版本

打开终端（Win + R → 输入 `cmd` → 回车）：

```bash
python --version
```

需要 **3.8 以上**。如果提示"不是内部或外部命令"，说明 Python 没装或没配环境变量，先去 [python.org](https://www.python.org/downloads/) 装一个，安装时**务必勾选 Add Python to PATH**。

### 第 2 步：创建虚拟环境

在 `AA` 目录下执行：

```bash
cd <仓库目录>
python -m venv .venv
```

这会生成一个 `.venv` 文件夹。看到它就成功了。

### 第 3 步：激活虚拟环境

```bash
.venv\Scripts\activate
```

激活后，命令行前面会出现 `(.venv)` 前缀：

```
(.venv) C:\code\test_demo>
```

**每次新开终端都要重新激活一次。** 忘了激活是新手最常见的坑——表现为"明明装了包却说找不到"。

> 如果提示"无法加载脚本，因为在此系统上禁止运行脚本"（PowerShell 报错），
> 换成用 `cmd` 终端，或者执行：`Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`

### 第 4 步：安装依赖包

```bash
python -m pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> `-i https://pypi.tuna.tsinghua.edu.cn/simple` 是换国内源，不加会非常慢。

装完验证：

```bash
pip list
```

**逐行比对这几个版本号**（课程文档里所有"实测数字"都是在它们下面跑出来的，
版本不一致时行为可能不同，尤其是 pytest 8 → 9 是跨大版本）：

```
requests             2.34.2
pytest               9.1.1
pytest-html          4.2.0
pytest-rerunfailures 16.6.1
pytest-xdist         3.8.0
PyYAML               6.0.3
jsonpath-ng          1.8.0
Flask                3.1.3
```

> `requirements.txt` 里已经按这几个版本 pin 好了。
> 如果 `pip list` 显示的版本不一样，多半是之前装过别的版本，重装一次：
> `pip install -r requirements.txt --force-reinstall -i https://pypi.tuna.tsinghua.edu.cn/simple`

### 第 5 步：启动练习接口服务

```bash
python mock_server\app.py
```

看到这个就成功了：

```
 * Running on http://127.0.0.1:5000
```

**这个窗口不要关**，后面所有练习都依赖它。把它最小化就行。

浏览器打开 http://127.0.0.1:5000 能看到接口列表。

---

## 三、验证环境

跑一下这个命令，全部 OK 就可以开始了：

```bash
python -c "import requests, pytest, yaml, flask; print('全部 OK')"
```

---

## 四、推荐编辑器

**VS Code**（免费）。装这两个插件就够了：
- Python（微软官方的）
- Chinese (Simplified) 中文语言包

没有 PyCharm 也完全不影响，不要在这上面纠结。

---

## 五、常见坑

| 现象 | 原因 | 解决 |
|---|---|---|
| `pip` 不是内部命令 | Python 没配环境变量 | 重装 Python 勾选 Add to PATH |
| 装包特别慢 / 超时 | 连的国外源 | 加 `-i https://pypi.tuna.tsinghua.edu.cn/simple` |
| 明明装了却 `ModuleNotFoundError` | 忘了激活虚拟环境 | 看命令行有没有 `(.venv)` 前缀 |
| `python` 打开的是微软商店 | 系统别名占用 | 用 `py` 命令，或去设置里关掉应用执行别名 |
| 启动 mock 提示端口被占用 | 5000 端口被别的程序占了 | 换端口：`python mock_server\app.py 5001` |

---

## 六、【面试考点】环境问题

面试官偶尔会问：

**Q：你怎么管理 Python 环境和依赖？**

答法：
> 用 venv 建独立虚拟环境，每个项目一个，避免包版本冲突。
> 依赖用 `pip freeze > requirements.txt` 导出，别人拿到项目 `pip install -r requirements.txt` 就能复现环境。

**Q：requirements.txt 和 Pipfile / poetry 有什么区别？**（进阶）

答法：
> requirements.txt 只能记录包和版本，区分不了直接依赖和间接依赖，也没有依赖冲突检查。
> poetry / Pipenv 能做依赖解析和锁定（lock 文件），更适合复杂项目。小项目用 pip + requirements.txt 够了。

---

环境好了，去 `day01-Python核心/教程.md` 开始 Day 1。
