# AA「5 天 Python 自动化面试冲刺」项目审查报告

> 审查视角：软件测试工程师 / 测试开发
> 审查方式：**逐条实测复现**，不采信文档自述。所有 P0 结论附可复现证据。
> 环境：Windows + 项目自带 `.venv`（Python 3.13.14 / pytest 9.1.1 / requests 2.34.2 / Flask 3.1.3）

---

## 0. 总体结论

**骨架是好的，测试专业性有系统性缺陷。**

这份材料的工程骨架扎实：分层清晰的数据驱动接口自动化框架、可运行、文档声明的测试数字我全部复现成功。它对自身局限的坦诚也超出预期（Day4 主动给出变量池的三条改进路径，Day5 明确要求「没落地就说没落地」）。

但有一个贯穿始终的问题：**这个项目在教测试，却在自己的示例里反复演示「没有真断言的测试」。**

| 示例 | 实际情况 |
|---|---|
| Day4 框架的 YAML 用例 | 漏写断言字段 → **判通过**（假绿） |
| Day2 签名课 6 个场景 | **零断言**，全是 `print` |
| Day2 数据库校验课 | 三项断言是**自证式**（自己插数据、再断言自己插的数据） |
| Day4 框架自身工具模块 | **零单元测试**，自测块只 `print` 不 `assert` |

对一份「面试冲刺」材料，这比任何单个技术错误都严重：学员照着练，练成的正是面试官要挑的毛病。

此外发现 **6 处教错的技术知识**（含 2 处会被金融/终端方向面试官当场标记的术语错误），以及 **3 处文档互相打架**。

---

## 1. 文档声明核对（全部实测）

### 1.1 声明的数字——全部成立 ✅

| 检查项 | 文档声明 | 实测 | 判定 |
|---|---|---|---|
| Day1 五个脚本 | 全部跑通 | 5/5 exit 0 | ✅ |
| Day2 五个脚本 | 全部跑通 | 5/5 exit 0 | ✅ |
| Day3 串行 | 49 passed, 2 skipped, 4 xfailed, 1 xpassed | 完全一致（收集 56 条） | ✅ |
| Day3 `-n 4` | 能并发跑通 | 通过 | ✅ |
| Day4 串行 | 25 passed | 25 passed | ✅ |
| Day4 连跑 3 次 | — | 25 passed ×3，**无 flaky** | ✅ |
| `--env=prod` / 无效环境名 | 友好提示 | 提示 + 退出码 1 | ✅ |
| 构建产物是否入库 | `.gitignore` 已配 | 53 个 tracked 文件，无 `.pyc`/日志/报告 | ✅ |
| Day4 `-n 2` | 修复记录称「退出码 1」 | **退出码 0**（静默降级为串行） | ❌ 见 P0-B5 |

### 1.2 环境不一致（P1）

`requirements.txt` 的 8 个版本 pin **全部**与实测环境不符：

```
声明                                实际安装
requests==2.32.3              →     requests 2.34.2
pytest==8.3.3                 →     pytest 9.1.1
pytest-html==4.1.1            →     pytest-html 4.2.0
pytest-rerunfailures==14.0    →     pytest-rerunfailures 16.6.1
pytest-xdist==3.6.1           →     pytest-xdist 3.8.0
PyYAML==6.0.2                 →     PyYAML 6.0.3
jsonpath-ng==1.6.1            →     jsonpath-ng 1.8.0
Flask==3.0.3                  →     Flask 3.1.3
```

`00-环境准备.md` 明确让学员 `pip install -r requirements.txt`，装出来却是另一套栈。文档里所有「实测数字」都来自**未 pin 的环境**。pytest 8 → 9 是跨大版本，风险不该被忽略。

---

## 2. P0-A · 教错的技术知识（必须修）

### P0-A1 `timeout` 语义讲错——面试官最爱追这条

**位置**：`day02-requests接口测试/教程.md:158`、`day02-requests接口测试/code/05_sign_and_exception.py:135`

> `timeout=5` 表示"连接 + 读取**总共**最多等 5 秒"

**这是错的。** requests 的单值 timeout 会同时赋给 connect 和 read（`HTTPAdapter.send` → `TimeoutSauce(connect=timeout, read=timeout)`），而 **read 超时是「两次收到字节之间的最大间隔」，不是总时长**。所以一个每 1 秒吐一块、总共吐 8 秒的响应，`timeout=5` 拦不住。

**实测**：用一个每 1 秒吐一块、共 8 秒的 drip 服务，`requests.get(url, timeout=5)` **正常返回，耗时超过 5 秒**。

**建议**：改成「requests 的 timeout 是 connect 和 read 两个独立超时，**没有请求级总时长超时**；要控总时长只能在用例层自己计时，或换 httpx（支持 `Timeout(total=...)`）」。这是一个能体现深度的加分点，讲错则直接暴露只背了 API。

### P0-A2 `contextmanager` 的异常路径讲错——且与自己的练习答案矛盾

**位置**：`day01-Python核心/教程.md:422`、`day01-Python核心/code/02_yield.py:148`

> `with` 块结束时，会自动执行 yield 之后的代码（**即使中间出异常也会执行**）

**这是错的。** `@contextmanager` 在 with 体抛异常时，会把异常 **throw 回 `yield` 那一行**；生成器若不捕获，`yield` 之后的代码**不会执行**，异常继续向外传播。只有包在 `try/finally` 里才能保证清理执行。

**实测**：

```
with db_connection() as c:      # 生成器：yield 后直接 print("3. 关闭数据库连接")
    raise RuntimeError(...)
→ 输出只有「1. 连接数据库」「2. 用 conn 查询数据」，第 3 行没打印
→ 加 try/finally 版本后，第 3 行正常打印
```

**而且同一套教材自相矛盾**：`day01-Python核心/练习与答案.md:169` 给出的结论是**正确**的。正文推翻了自己的练习答案，学员会无所适从。

**建议**：改写 `教程.md:422` 与 `02_yield.py:148`，并给 2.5 节的示例补上 `try/finally`——顺带正好讲清「为什么 fixture 的 teardown 要写成 yield 之后的代码（pytest 帮你做了 try/finally）」，这比现在的说法有价值得多。

### P0-A3 `day02` 6.2 节的示例**永远不会触发**

**位置**：`day02-requests接口测试/教程.md:451-458`

```python
try:
    requests.get(url + "/api/error")
except requests.exceptions.RequestException as e:
    print(f"请求异常：{e}")
```

`/api/error` 返回的是 **HTTP 500 + text/html**（已实测）。**requests 不会因为 4xx/5xx 抛异常**，所以这个 `except` 永远不会执行——示例在教一个不存在的现象。

**更糟的是同一篇教程 6.3 节自己讲了 `raise_for_status()`**，也在别处写了「requests 不会因 404/500 抛异常」。等于前后打脸。

**建议**：示例改为先 `resp.raise_for_status()` 再捕获 `HTTPError`。

### P0-A4 `Session` 根本没有「默认 timeout」这个能力

**位置**：`day02-requests接口测试/教程.md:174`

> `requests.Session()` 帮你做三件事：…… ③ 统一设置默认的 headers、**timeout**

**实测**：`Session` 对象没有会被 requests 使用的 timeout 属性。

```
requests.get(url, timeout=1)         → Timeout @ 1.0s   （服务器确实卡 3s）
s = requests.Session(); s.timeout=1
s.get(url)                           → 成功返回 200，耗时约 5.0s  ← timeout 被完全忽略
```

headers 那半句是对的（`s.headers.update(...)` 有效），timeout 这半句是错的。

这恰好是「**你怎么统一管理超时**」这个标准追问的落点。**建议**：改成「Session 能统一 headers 和 cookie，但**不能**统一 timeout；要统一超时得继承 Session 重写 `request` 做 `kwargs.setdefault("timeout", ...)`」——而项目自己的 `request_util.py:76` 正是这么做的（`kwargs.setdefault("timeout", self.config.timeout)`），是现成的正确示范，只是教程没把它和这个知识点连起来。

### P0-A5 JWT / JWE / JWS 术语混淆——金融与终端方向面试官的「一秒标记点」

**三处同源错误**：

| 位置 | 原文 | 问题 |
|---|---|---|
| `day02/教程.md:227` | 「JWT：一种 token 格式，把用户信息**加密**在 token 里」 | JWT 默认是 JWS——**只签名不加密，payload 是 base64url，直接可读**。加密是 JWE |
| `day02/code/05_sign_and_exception.py:5` | 「你在华为钱包做过的 **JWE 签名**」 | JWE 是加密标准（RFC 7516），签名是 JWS（RFC 7515） |
| `day05/项目介绍话术.md:24,41` | 「报文做 **JWE 签名**」/「RSA 私钥签名，华为侧验签」 | 同上；且「RSA 私钥签名」描述的正是 **JWS/RS256**，不是 JWE |

参见 [JOSE 各标准区别（JWA/JWE/JWK/JWS/JWT）](https://stackoverflow.com/questions/74257560/what-is-the-difference-between-jose-jwa-jwe-jwk-jws-and-jwt/74257561) 与 [Visa 的 JWE/JWS 说明](https://developer.visa.com/pages/encryption_guide/jwe-jws)。

**最讽刺的是项目自己讲对了**：`day02/code/05_sign_and_exception.py:49` 白纸黑字写着

> 注意：**签名 ≠ 加密**。签名防篡改，加密防窃听，两回事。

而 `day05/项目介绍话术.md` 的标题是「**可直接背**」。学员背下这句，等于在面试里把一个术语说错，并和自己刚学的内容打架。**建议**：三处一起改成「JWS 签名（JWE 用于加密）」；JWT 那条补上「所以 JWT 里不能放敏感信息，靠签名防篡改、靠 HTTPS 防窃听」。

### P0-A6 mock 把期望签名回显在错误信息里 → 签名课的「防篡改」演示可被绕过

**位置**：`mock_server/app.py:264`

```python
if sign != expect:
    return fail(4003, f"签名错误，期望 {expect}")     # ← 把正确签名告诉了调用方
```

**实测（完整绕过链）**：

```
① 篡改 amount 后带旧签名发送 → {'code': 4003, 'msg': '签名错误，期望 15dae6cbf456e7191a341c4f35617aaf'}
② 从错误信息里读回「期望签名」= 15dae6cbf456e7191a341c4f35617aaf
③ 用这个签名重发同样的篡改请求 → {'code': 0, 'data': {'msg': '验签通过'}}
```

也就是说，`day02/code/05_sign_and_exception.py:93-97` 想演示的「改了参数 → 签名对不上 → 服务端拒绝」，**照着错误信息就能绕过**。这不只是教学瑕疵——让被测服务回显期望签名，本身就是一个真实的安全缺陷，而这门课的主题正是「签名防篡改」。

**建议**：mock 错误信息只回「签名错误」，不回显 `expect`；并在教程里补一条「故意用错算法算签名 → 必须被拒」的用例。

---

## 3. P0-B · 产出假绿的测试缺陷（必须修）

这一组的共同主题：**一次错误的输入或一次服务端异常，都能得到一条绿色的 `passed`。**

### P0-B1 YAML 用例漏写或拼错 `validate` → **静默判通过**

**位置**：`framework/common/assert_util.py:114-115`、`framework/common/case_runner.py:68-71`

```python
def do_validate(resp, validate_list, case_name=""):
    if not validate_list:
        return          # ← 没有断言 = 直接返回成功
```

`case_runner.py` 用 `case.get("validate", [])` 取值，字段名写错就取到空列表，于是**零断言执行**，随后照常打日志 `用例执行通过`。

**实测**：

| 场景 | 输入 | 结果 |
|---|---|---|
| A | 用例完全没有 `validate` 字段 | **通过**（0 条断言执行） |
| B | 字段名笔误写成 `validates`，且**密码故意写错**（服务端返回 `code=1003`） | **通过** |

场景 B 最危险：一条业务上必然失败的用例，因为一个字母的拼写差异，在报告里是一条绿的 `passed`。

**为什么是 P0**：这套框架的核心卖点是「加用例只改 YAML，不用写代码」，而这个自由度**没有任何 schema 校验兜底**。教程把 `validate` 描述为必填字段（`day04/教程.md:312`）却从不校验。**建议**：在 `run_case` 入口校验用例结构（缺 `validate`、出现未知字段即报错），或至少 `validate` 为空时 `pytest.fail`。

### P0-B2 「字段为 null」与「字段不存在」无法区分 → `is_null` / `ne` 假通过

**位置**：`framework/common/assert_util.py:23-31,34-47`

JSONPath 命中但值为 `null`，与路径写错查不到，**都返回 `None`**。后果：

- **实测**：`is_null: ["$.data.non_exist_field"]` 在 `/api/user/1` 上**通过**——字段根本不存在，却断言「为空」成功
- `ne` 配错路径（如 `$.data.tokenn`）→ `None != 期望值` → **必然通过**，比零断言更隐蔽
- `not_null` 反而正常（能查出路径错），说明问题只在「存在性语义」缺失

**建议**：`extract_by_jsonpath` 区分「未匹配」与「值为 null」（未匹配抛错或返回哨兵值）。

### P0-B3 `extract` 提取失败只打 WARNING，用例仍判通过 → 根因被藏到下游

**位置**：`framework/common/case_runner.py:59-65`

**实测**：提取一个不存在的字段 → 日志出现 WARNING，用例**判通过**，变量池为空。

后果是典型的「错误跑到下游才炸」：上游用例因字段名写错没提到值，它自己是绿的；下游报 `KeyError: 变量 ${order_id} 不在变量池中`——**报错位置指向受害者，不指向肇因**。

值得注意的是：`修复记录-20260914.md:19` 把这条 `KeyError` 归因为并发问题。并发确实是原因之一，但**这条错误在完全串行、单条执行时同样会出现**（见 P0-B5 下的实测），说明根因不止一个，这一条被漏掉了。

**建议**：`extract` 失败应直接让用例失败——提取不出值 = 该用例前置未满足。

### P0-B4 响应体不是 JSON 时，业务断言抛 `JSONDecodeError` 而不是干净的断言失败

**位置**：`framework/common/assert_util.py:45-46`（`_resolve` 对 `$` 开头表达式直接调 `resp.json()`）

**实测**（`GET /api/users?page=abc` → HTTP 500 + text/html）：

```
断言 status_code  →  干净的断言失败：期望 400，实际 500        ✅
断言 $.code       →  JSONDecodeError: Expecting value: line 1 column 1  ❌
```

**服务端吐了一个 HTML 错误页，框架把它呈现成「框架自己崩了」**——恰好违背 `conftest.py:62-63` 自己写的原则：「把『用户用错了』和『框架坏了』区分开」。初学者会去怀疑框架或断言语法，而真正的问题是被测服务 500 了。

**建议**：解析 JSON 前判断 `Content-Type` / 捕获解析异常，转成「响应不是合法 JSON（HTTP xxx，body 前 200 字符）」的断言失败。

### P0-B5 `-n` 的拦截行为有**三种互相矛盾的说法**，且拦截逻辑过宽

**位置**：`framework/conftest.py:66-72`；`修复记录-20260914.md:39`；`day05/追问清单与答法.md:99,168-170`

| 来源 | 说法 | 实测 |
|---|---|---|
| `conftest.py` 实际行为 | 打印提示后**静默降级为串行**，正常跑完 | **退出码 0** |
| `修复记录:39` | 「友好提示 + **退出码 1**」 | ❌ 实际 0 |
| `追问清单:168-170` | 「现在跑 `-n` 会**直接失败**」 | ❌ 实际不失败 |
| `day04/教程.md:379` | 「`-n` 已被忽略，按串行执行，用例照常跑完」 | ✅ 唯一正确的表述 |

**为什么必须修**：`追问清单` 是拿来背的面试速查。学员说「我的框架跑 `-n` 会直接失败」，面试官说「那你跑一下」——屏幕上显示 `25 passed`。而真实发生的是**静默降级**：CI 上配了 `-n 4`，构建是绿的，没人知道并发根本没生效。**这比直接失败更危险，也比「直接失败」更有讲头。**

**另外，拦截逻辑本身过宽**：`conftest.py:67` 只要检测到 `-n` 就无条件清空 `numprocesses`，**完全不看 `--dist`**。我在副本中禁用拦截后实测：

```
--dist=loadscope -n 2  →  created: 2/2 workers  →  25 passed   ✅ 真并发成功
默认 load -n 2         →  created: 2/2 workers  →  2 failed    ❌ 如文档所述失败
```

原因：本框架所有跨用例依赖**恰好都发生在同一个文件内**（`order_id` 在 `test_order.yaml` 内流转、`created_user_id` 在 `test_user.yaml` 内流转），而 `loadscope` 正是按模块分组的调度。

所以 `day04/教程.md:404` 那句「**任何**『靠进程内全局变量传数据』的框架，**都不可能**直接支持多进程并发」**绝对化过头**了，会被「你试过 `loadscope` 吗」一句反将。准确说法应是：

> 默认的 load 调度下不行（依赖会跨进程）；按模块分组（`loadscope`）在本框架上恰好可行，因为依赖都收敛在单文件内——但这是脆弱的巧合，依赖一旦跨文件就会崩。所以我默认关掉并发，把它当成已知的架构约束。

**顺带**：变量池的顺序依赖破坏的不只是 `-n`。**实测单独执行「支付订单」用例**（跳过前置的创建订单）：

```
KeyError: "变量 ${order_id} 不在变量池中，当前已有：['test_user_id']"
```

下列日常操作全部会踩：单条执行 / `-k` 筛选、`pytest --lf`（依赖生产方可能不在失败集合里）、失败重试、随机化顺序插件。而 `requirements.txt` 里装了 `pytest-rerunfailures`，**框架恰恰不能安全重试**（重试一条**支付**用例意味着重复支付）。这一点没有任何地方提醒。

### P0-B6 两节「教测试」的课自己不用断言

| 文件 | 问题 |
|---|---|
| `day02/code/05_sign_and_exception.py:64-108` | `demo_sign_ok` / `demo_sign_fail` **零断言**，全部 `print(r.json()['msg'])`。文件自己在 110-115 行列出了「签名接口要覆盖的场景」（正确签名／参数篡改／签名缺失／时间戳过期／重放），**5 个场景没有一个是断言出来的**——签名错了脚本照样 exit 0。只能证明「我的实现等于我的实现」。 |
| `day02/code/04_db_check.py:138-156` | 「接口成功 ≠ 落库」这一课的断言是**自证式**：脚本自己 `INSERT ... "CREATED"`（第 141 行），紧接着 `assert row[0] == "CREATED"`（第 153 行）。**这个断言不可能因为「系统没落库」而失败。** 而项目里根本没有数据库（mock 的 `ORDERS` 是内存字典），教程里「下单后查数据库确认订单存在」的练习也**无法执行**。 |

`04_db_check.py:138` 有注释说明「真实项目里这一步由被测系统完成」，态度是诚实的；但输出仍打印「④ 三项断言全部通过」，给初学者的印象是「这就是数据库校验」。

**建议**：让 mock 真正落 SQLite，这两节课的断言才有意义；或至少把签名课的 `print` 改成 `assert`，并在 db 课明确写「本课无真实数据库，只演示套路」。

### P0-B7 `safe_request` 把「HTTP 通」当成「业务对」

**位置**：`day02/code/05_sign_and_exception.py:174-218`

`safe_request` 只按 `resp.raise_for_status()` 判 `[PASS]`。而 mock 的业务失败**全是 HTTP 200**：

```
密码错误    → HTTP 200 + code 1003   → 被判 [PASS]
验签失败    → HTTP 200 + code 4003   → 被判 [PASS]   （已实测）
```

这与同一份教程 `:135`「只判断 200 会漏掉大量业务 bug」、追问清单 Q5 的「两层断言」**直接矛盾**。而这个函数被注释成「框架里就该这么写」，学员会把它当范本。

**建议**：拆成两层——`request_ok`（传输层）+ 业务断言，并在教程里点明「HTTP 通 ≠ 用例通过」。

---

## 4. P1 · 面试有风险 / 工程不严谨

### P1-1 进阶的 Day4 在断言规范上**比基础的 Day3 退步了**

这是我认为最值得作者重视的一条。Day3（基础课）教得很好：

```python
# day03/code/test_api.py
assert r.status_code == 200, f"HTTP 状态码异常：{r.status_code}"   # 两层断言
assert body["code"] == 0
assert abs(r.json()["data"]["amount"] - 99.9) < 0.01              # 金额近似比较
def test_no_token():                                              # 401 安全用例
    r = requests.get(f"{BASE_URL}/api/user/1")
    assert r.status_code == 401
```

而 Day4（进阶课，README 标注「**最高，决定薪资**」）的 YAML 里：

- `status_code` 只在 6 条登录用例中的 2 条、7 条订单用例中的 1 条出现
- 金额改成了精确相等：`data/test_order.yaml:25` → `eq: ["$.data.amount", 99.9]`
- **「无 token」安全用例消失了**——25 条用例没有任何一条覆盖 401 路径

金额那条尤其刺眼：`追问清单 Q24` 斩钉截铁说「金额绝不用 float，比较用差值 `abs(a-b)<0.01`」，`day03` 也正是这么做的——**到了 Day4 自己反着做**。当前能过只因 mock 把 `99.9` 原样回传，属于运气。

**建议**：Day4 的 YAML 至少补上 `status_code`、近似金额断言、401 用例——甚至可以考虑给 `ASSERT_MAP` 加一个 `approx` 算子。否则「越学越松」。

### P1-2 框架里那颗「定时炸弹」修了因，却没加回归测试

`修复记录` 的 P2 项把「fixture 用 `RequestClient()` 新建、业务层用 `get_client()` 单例」定性为定时炸弹，症状是「未登录 → 401，报错指不出原因」。修复是把 fixture 改成单例——**但没留下任何测试防止复发**。

同时 `追问清单 Q32` 让学员回答「覆盖了……安全（**无 token**）」，`Q36` 的用例设计四层里第四层也是「安全：不带 token、token 过期、签名错误」。**Day4 框架里一条都没有。** 说的比做的多，面试官问「无 token 那条用例在哪」就穿帮。

### P1-3 框架自身工具模块零单元测试

`var_pool.py` / `request_util.py` / `config.py` / `assert_util.py` 的 `if __name__ == "__main__"` 自测块**全是 `print`，没有一个 `assert`**：

```python
# var_pool.py:77-84
print(VarPool.replace({"userId": "${user_id}"}))    # 期望 {'userId': 5}
```

结果对不对要靠眼睛看。`修复记录` 的「24 项检查全部通过」也是人工目测，不是自动化回归。

**对一份教测试的项目**，这恰恰是最该示范的地方：`common/` 下的纯函数（变量替换、JSONPath 取值、断言算子）最容易也最值得写单元测试，而项目一条都没写。

### P1-4 mock 作为「被测系统」对非法输入过于脆弱

实测 `mock_server/app.py`：

| 输入 | 结果 |
|---|---|
| `GET /api/users?page=abc` | **HTTP 500 + HTML** |
| `GET /api/users?size=xyz` | **HTTP 500 + HTML** |
| `POST /api/login` 带非 JSON 体 | **HTTP 500 + HTML** |
| `/api/sign/verify` 带 `timestamp: "abc"` | **HTTP 500 + HTML** |
| `POST /api/order` 带 `amount: "abc"` | HTTP 200 + `code=3002`（唯一优雅处理的） |

`page`/`size` 直接 `int(request.args.get(...))`（`:175-176`）、`timestamp` 直接 `int()`（`:256`），没有输入校验。

后果：① 学员想做「参数校验」类负向测试（`追问清单 Q36` 第二层「必填、类型、长度、边界」）时会撞上被测系统 500，而不是可断言的结构化错误；② 结合 P0-B4，这类 500 会让框架抛 `JSONDecodeError`，把水搅浑。

顺带：`app.py:67-83` 的首页接口清单**漏列了 `/api/order/<id>/pay`**，而这是 Day3/Day4 在用、教程在讲的核心接口。

### P1-5 教程的依赖示例变量名写错，照抄必崩

`day04/教程.md:323`：

```yaml
      userId: ${user_id}      # ← 自动替换成真实值
```

框架真实变量名是 `test_user_id`（`framework/conftest.py:168` 的 `VarPool.set("test_user_id", user_id)`）。学员照抄会直接 `KeyError`，而报错把锅甩给变量池。

### P1-6 简历话术存在「替学员编造细节」的风险

`项目介绍话术.md:14-29` 的 1 分钟版本是第一人称的具体公司项目（华为钱包卡券服务），含 JWE 签名、五区域环境、CloudTest 平台等细节。文件开头要求「方括号内容必须换成你自己的真实数据，不要照抄也不要编」，**第 41 行却已经替学员把技术细节写好了**（RSA 私钥签名、构造错误报文验证服务端拒绝）。

这与该文件自己第五节「绝对不要说的话」（`:151-158`）和「编造的细节一追问就穿」的立场**自相矛盾**——而 P0-A5 的 JWE 术语错误正是「一追问就穿」的活样本。

**建议**：把「可能追问」表里的具体技术答案改成**提示性问题**（「你那个签名是签名还是加密？用什么算法？」），让学员用自己的真实经历填。

### P1-7 Day5 教的「工程化」在仓库里没有产物

- 仓库里**没有任何 CI 配置**（无 `Jenkinsfile`、无 `.github/workflows`、无 `.gitlab-ci.yml`）——`git ls-files` 确认
- `requirements.txt` **没有 `allure-pytest`**，`day05/教程.md:159-188` 的 Allure 部分实际不可运行
- `提速计划.md:107` 又说「Day5 的 Jenkins / Allure 实操——面试问到能说出概念就够，不用真搭」，与 day05 教程「给你命令让你做」的写法冲突

而真实面试里，**「Python + Pytest + Allure + Requests 各自角色」经常是第一道题**（见 [pytest 接口自动化面试问题汇总](https://www.cnblogs.com/KnowledgePlanet/p/19225393)）。一个连 `allure-pytest` 都没装、没有 CI 文件的「工程化日」，撑不住这一问。

### P1-8 Jenkinsfile 写死 Windows，Linux CI 直接失败

**位置**：`day05/教程.md:108-109,116`

```groovy
bat 'python -m venv .venv'
bat '.venv\\Scripts\\pip install -r requirements.txt'
bat '.venv\\Scripts\\python day04-搭建框架\\framework\\run.py --env=test'
```

`bat` 是 Windows 专属步骤，`.venv\Scripts\` 是 Windows 路径，而实际 CI 节点以 Linux 为主（对应 `sh` + `.venv/bin/`）。学员照抄到公司 Linux 节点会直接失败。

另外第 116 行的 `day04-搭建框架\framework\run.py` 是**教学仓库的目录结构**，不是真实项目结构，照抄到公司项目必然要改。教学示例至少该提示这一点。

### P1-9 `day03` 教程代码片段与实际文件不一致

`day03/教程.md:275-279` 的 parametrize 片段只有 4 组数据，并说「一份逻辑，4 条数据……生成 4 个用例」；而实际文件 `day03/code/test_parametrize.py:16-22` 有 **5** 组——多了一条 `("a", "123456", 1002)`（用户名太短，边界用例）。教程与代码差一条，学员对照时会对不上。

---

## 5. P2 · 小瑕疵

| # | 位置 | 问题 |
|---|---|---|
| P2-1 | `assert_util.py:82-89,122-130` | `is_null`/`not_null` 只用 1 个参数，但传第 2 个时被**静默忽略**，不报错 |
| P2-2 | `run.py:49-52` | 因环境名错误而 0 条用例执行时，打印「存在失败用例，退出码 1」——措辞不准，会让人找不存在的失败用例 |
| P2-3 | `data/test_order.yaml:25` | 金额用 `eq` 精确比较浮点；`ASSERT_MAP` 里没有近似比较算子可用（与项目自己的 Q24 教学冲突） |
| P2-4 | `data/test_user.yaml:26-64` | 「删除刚创建的用户」依赖前面「创建用户成功」提取的 `created_user_id`——清理**依赖前置用例成功**，前置失败就留脏数据。无条件清理只有 `conftest.py` 的 session 用户 |
| P2-5 | `day04/教程.md:355` | 把「失败重试」列为待加能力，但 `request_util.py:59-109` 早已实现传输层重试。且未讲清「重试只覆盖超时/连接错误，不覆盖断言失败」与「写操作重试的危险」 |
| P2-6 | `day04/教程.md` 目录树/正文/话术 | 「分五层」的三处表述不一致（配置/公共/请求封装/业务/用例数据 vs 配置/公共/业务 API/用例数据/用例执行） |
| P2-7 | `day02/code/04_db_check.py:43` | 建表用 `amount TEXT`，与同一课「金额用 DECIMAL 或分为单位整数」的建议相悖 |
| P2-8 | `day02/code/04_db_check.py:152-155` | 用裸 `assert`，`python -O` 下会全部失效，未提示 |
| P2-9 | `day01/code/02_yield.py:140,167` | 使用已废弃的 `tempfile.mktemp`（存在竞态安全隐患） |
| P2-10 | `day01/code/02_yield.py:64-69` | 用 `sys.getsizeof` 对比「列表 vs 生成器」的内存，`getsizeof` 不统计元素，差距被严重低估 |

---

## 6. 值得肯定的地方（修改时不要一起改掉）

1. **声明与实测一致**。README 的 `25 passed`、`49 passed, 2 skipped, 4 xfailed, 1 xpassed`、Day3 可并发，我全部复现成功。这在教学项目里不常见。
2. **Day4 教程对自身缺陷的坦诚**（`day04/教程.md:406-422`）：给出 fixture 传值／外部存储／用例自包含三条改进路径，明确推荐「自包含打底 + 组合场景写进同一条用例」，并指出 `test_api_style.py::test_full_business_flow` 就是这个正确做法。这一段质量很高。
3. **面试话术守住了诚信底线**。`项目介绍话术.md:151-158`「绝对不要说的话」把「框架是我从零写的（如果是参考的）」「我们公司项目用了我这套框架（如果没用）」明确列为禁区；`:76-77` 主动要求说「这个项目没在公司落地，是自学练手的」。这个底线意识应在修改时保留。
4. **Day5 追问清单整体质量高**：`Q28 你的框架有什么不足` 主动要求暴露真实缺陷；`Q40 怎么衡量自动化做得好不好`（误报率／有效性／维护成本／执行时间）比只谈覆盖率成熟得多；`Q14 用例不稳定怎么办` 明确指出「重试会掩盖问题，重试通过的用例要单独统计」。
5. **工程卫生到位**：`.gitignore` 覆盖 `.venv`/`__pycache__`/`logs`/`reports`/`.pytest_cache`，53 个入库文件里没有任何构建产物或日志。
6. **用例稳定**：Day4 串行连跑 3 次全部 25 passed，未见 flaky。
7. **Day3 的用例质量明显高于 Day4**——两层断言、金额近似比较、401 安全用例、幂等性验证都做对了（问题在于 Day4 没继承它们）。
8. **`day02` 的 JSONPath 与变量池「整串占位符保类型」一节**是全套材料里设计最扎实的部分，讲清了「int 变 str 导致服务端匹配不上」这个真实坑。

---

## 7. 建议的修改顺序

**第一批 · 教错的（会误导学员，最伤可信度）**
1. `timeout` 语义（P0-A1）
2. `contextmanager` 异常路径 + 与练习答案的矛盾（P0-A2）
3. `day02` 6.2 永不触发的示例（P0-A3）
4. `Session` 默认 timeout（P0-A4）
5. JWT / JWE / JWS 术语三处（P0-A5）
6. mock 回显期望签名（P0-A6）

**第二批 · 假绿（最伤「测试」这个主题）**
7. `validate` 空/拼错 → 报错而非通过（P0-B1）
8. `extract` 提取失败 → 用例失败（P0-B3）
9. JSONPath 未匹配与 `null` 区分开（P0-B2）
10. 非 JSON 响应给干净报错（P0-B4）
11. 签名课/数据库课补真断言（P0-B6）、`safe_request` 拆两层（P0-B7）

**第三批 · 文档打架（直接影响面试）**
12. 统一 `-n` 的三处说法；话术改成「静默降级比失败更危险」（P0-B5）
13. 修 `day04/教程.md:323` 的 `${user_id}`（P1-5）
14. 改写「可能追问」里替学员编造的细节（P1-6）

**第四批 · 专业性补齐**
15. Day4 的 YAML 补 `status_code`、近似金额断言、401 用例（P1-1、P1-2）
16. 给 `common/` 下的纯函数写真正的单元测试（P1-3）
17. mock 补输入校验 + 补首页接口清单（P1-4）
18. `requirements.txt` 与实测环境对齐（第 1.2 节）
19. 二选一：补可运行的 Allure + CI，或把 Day5 明确降级为概念课（P1-7、P1-8）

---

## 8. 教学法层面的两点建议

1. **缺一条「用例设计」主线。** mock 里现成有 13 个业务码（1001/1002/1003/2001/2002/2003/3001/3002/3003/3004/4001/4002/4003）、现成的边界（username 2–20 位、phone 11 位、amount > 0、重复支付保护 code 3004），但 Day2 几乎没有用例设计教学——全是「跑一遍改参数」。**这是 5 天冲刺里最值钱的内容**：等价类、边界值、鉴权矩阵、幂等、异常输入。建议加一节，并自然衔接 Day3 的参数化。
2. **重心略偏。** `-n` 并发一个点讲了三遍（教程约 1/9 篇幅 + conftest 注释 + 追问清单），而「测试数据生命周期、用例可重复执行、断言规范、报告怎么读」这些高频题都只有一行。建议 Day4 压缩并发篇幅，换成数据生命周期 + 断言规范 + 环境防护。

---

## 9. 关于本报告的说明

- 所有 P0 结论均由我在本机**实测复现**，附命令与原始输出；P1/P2 中凡属代码/文档阅读推导的已注明。
- 审查过程中使用了两个辅助审阅代理。其产出中有 **4 条经我复核为错误**，已剔除或修正，列明如下：
  1. 「`-m smoke` 只收集到 1 条」——**实测 7 条**（登录模块 6 条 + `test_api_style` 1 条）。有效部分仅是：order/user 的 15 条 YAML 用例无 smoke 标记，且 YAML 无 marker 字段可用。
  2. 「`--dist=loadscope` 实测 25 passed 因而推翻教程断言」——其原始证据**无效**：那次加了 `-q --no-header`，把拦截提示隐藏了，实际仍是串行。我用**禁用拦截的副本**重测，`created: 2/2 workers` 下确为 25 passed，结论成立但证据换成本报告的。
  3. 「`flow_user` 每轮新建不清理」「`yaml_case_user` 不清理」——**错误**：前者在 `test_api_style.py:87-89` 的 `finally` 里删除，后者由 `test_user.yaml:58-64` 的用例删除。真实问题只是「清理依赖前置用例成功」（已收录为 P2-4）。
  4. 把 `is_null` 多余参数被静默抛弃列为 P0——**我降级为 P2-1**（影响面远小于其它假绿路径）。
- 验证过程中产生的临时脚本、探针与副本目录**已全部删除**，未修改课程仓库的任何文件。
- 工作区残留 3 个**空目录**（`_tmp3`、`_tmp4`、`_tmp_xdist`），由 pytest 的 basetemp 机制创建并带只读 ACL，当前沙箱下无法删除。它们是空目录，git 不追踪，可手动删除。
